import 'package:flutter/material.dart';
import '../styles/my_account_style.dart';

class MyAccountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Account Page', style: kPageTitleStyle),
    );
  }
}

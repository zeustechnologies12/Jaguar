import 'package:flutter/material.dart';
import '../styles/home_tab_style.dart';

class HomeTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Welcome to the Home Tab!', style: kPageTitleStyle),
    );
  }
}

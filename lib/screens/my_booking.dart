import 'package:flutter/material.dart';
import '../styles/my_bookings_style.dart';

class MyBookingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Bookings Page', style: kPageTitleStyle),
    );
  }
}
